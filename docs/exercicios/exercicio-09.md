# Exercícios 09 - C e C++ 🧱

!!! tip "Objetivo"
    **Objetivo**: Dominar a memória e a performance.

---

## 🟢 Fáceis

1.  **Tabuada em C**: Crie um programa em C que leia um número e mostre sua tabuada usando `printf`.
2.  **Média Escolar**: Leia 3 notas (float), calcule a média e mostre com 2 casas decimais (`%.2f`).

## 🟡 Médios

3.  **Classe Retângulo (C++)**:
    *   Crie uma classe `Retangulo`.
    *   Atributos: `largura`, `altura`.
    *   Método: `calcularArea()`.
    *   No `main`, crie um objeto, defina valores e mostre a área.
4.  **Inversão com Ponteiros (C)**:
    *   Crie uma variável `int x = 10`.
    *   Crie um ponteiro `int *ptr = &x`.
    *   Mude o valor de `x` para 20 **usando apenas o ponteiro**.

## 🔴 Desafio

5.  **Gerenciamento de Alunos (Structs)**:
    *   Defina uma `struct Aluno` com `nome` (char[50]) e `nota` (float).
    *   Crie um vetor de 3 alunos.
    *   Preencha os dados.
    *   Percorra o vetor e mostre apenas os alunos com `nota >= 7.0`.